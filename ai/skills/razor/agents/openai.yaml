interface:
  display_name: "Razor"
  short_description: "Shrink merge-base diff with explicit tradeoffs"
  default_prompt: "Use $razor to minimize my local diff against merge base with behavior-preserving refactors first, then increasing-loss options."
